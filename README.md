# WAF-w-nginx-przy-uzyciu-DOCKER
Projekt przedstawia przykładową implementację Web Application Firewall (WAF) opartą na NGINX, ModSecurity v3 oraz OWASP Core Rule Set, uruchomioną w środowisku Docker na maszynie wirtualnej zarządzanej przez Vagrant.
